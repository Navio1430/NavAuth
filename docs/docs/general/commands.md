# Commands & Permissions

## Default permissions
If you want to disable usage of default permissions,
you can set the permission value explicitly to false in your permissions plugin (e.g., LuckPerms).
For example, to restrict `/register` command set `navauth.user.register` value to `FALSE`.

## Available commands
<!--@include: ../generated/velocity-commands.md-->
